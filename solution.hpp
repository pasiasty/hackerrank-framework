#include <algorithm>
#include <iostream>
#include <map>
#include <regex>
#include <set>
#include <string>
#include <vector>

inline void solution(std::istream &in = std::cin, std::ostream &out = std::cout) {
  std::string line;
  while (std::getline(in, line))
    out << line;
}